> AI 驱动开发方法论 ｜ 第三部分 · Plan 规划
> 目录见 [README](README.md)

# 把方案拆成 Agent 能领取的任务与交付物追踪

在方案锁定与设计文档就绪后，规划阶段的最后一环是将系统设计切分为逻辑独立、职责单一的任务模块。这道切割工件直接决定了下一步并行 Worktree 开发的吞吐上限。如果划分边界模糊、多项任务存在重叠，并行的 Agent 会因为在同一物理代码块上修改冲突而造成灾难性的覆盖与混乱。

**方案必须拆解为物理边界清晰、单一职责的原子任务，并通过契约先行拦截多 Agent 并行冲突。** 

只有当任务的物理边界被清晰定义，且具备明确的输入与输出时，我们才能放心地将任务授予 Agent 自主完成，实现真正意义上的“监督式自治”。


## 原子任务拆解：规避越界冲突

AI 驱动开发的高吞吐红利建立在“并行 Worktree”的机制上。但若没有在前置阶段将方案合理地进行物理切分，并行就会成为代码冲突的制造器。

如果我们将“开发自动退款功能”作为一个大包袱直接塞给单个 Agent，它不仅会因为任务过重而在超长上下文会话中迷失，也阻断了多线并行的可能性。正确的做法是将其解耦拆分为多个无物理交叠的子任务：

* **退款风控判定逻辑**：纯算法计算，输入退款上下文，输出分类决策（自动/审批/拦截），其修改范围限制在独立的领域决策层。
* **审批申请状态机**：维护 `ApprovalRequest` 实体，状态变更为 `Pending` 挂起、`Approved` 审批通过以及 `Rejected` 驳回。
* **外部出款调用与 Saga 回滚**：调用第三方支付网关接口，处理网络重试与金额冲正。

对于每一个子任务，必须使用硬性不变量约束其修改范围（如“本任务仅允许改动 `src/domain/refund/decision.ts`”）。一旦任务具备了单一职责和可独立验收的特质，Agent 就不会在不相关的模块中越界改动，从而确保并行线之间互不串台。


## 任务粒度的三个判断标准

任务拆解的核心挑战在于规避两个极端：因粒度过细而使 Agent 失去上下文，或因粒度过粗而导致逻辑爆炸。评估粒度合理性的三项核心指标如下：

1. **可独立验证性**：任务完成后，必须产生可被单独验证的交付物（如一个可直接执行测试的 API endpoint、一个具备单元测试的 Service 方法、或一个可在 Storybook 中呈现的 UI 组件）。如果任务颗粒度小到无法单独验证，Agent 就会因为缺乏局部反馈信号而在编码时失去方向。
2. **架构分层独立性**：单个任务的修改范围不应跨越多个物理分层（如同时修改数据库 Schema、核心领域逻辑与 UI 展现）。跨层修改会成倍提升 Agent 的决策复杂度，导致 Pull Request 产生冗余 Diff，推高人工评审成本。
3. **上下文完备性**：任务描述中所含的输入契约必须足够完备，确保 Agent 无需在执行中频繁询问“这里该调用哪个外部接口”或“该字段是否应该持久化”。频繁的交互中断说明前置设计决策尚未收敛，任务不具备下发条件。

以 Relay 的退款场景为例：
* **反面典型（粒度过细）**：“给 `RefundService` 类加一个 `validateAmount` 方法。” Agent 无法得知其调用来源、校验上下文及超限时的异常设计，无法为其编写有意义的单元测试。
* **反面典型（粒度过粗）**：“实现 Relay 的自动退款。” 任务横跨表结构变更、退款核心逻辑、第三方支付网关调用、前端确认交互及通知服务五个层面，改动范围失控，系统难以设置局部门禁。
* **优秀实践（粒度合适）**：
  ```
  实现 RefundService.processRefund 方法。
  
  入参：RefundRequest（含 conversation_id: string、amount_cents: number、reason: string）
  出参：RefundResult（含 refund_id: string、status: 'pending' | 'approved' | 'rejected'）
  
  金额超过 50,000 分时抛出 RefundLimitExceeded 异常。
  接口类型定义已在 src/types/refund.ts 里锁定，直接 import，不要改动类型文件。
  本任务修改范围：src/services/RefundService.ts 及对应的单元测试文件。
  ```


## 跨任务依赖：接口契约先行

当 Task A 的输出是 Task B 的输入时，依赖关系处理不当会让并行任务互相等待，或者产生更危险的情况——两个 Agent 在各自的 Worktree 里对同一接口做出了不同的假设，集成时才发现对不上。

Relay 的退款流程里，`RefundService`（Task A）完成后需要调用 `NotificationService`（Task B）发送退款成功通知。如果同时启动这两个任务，两个 Worktree 合并时极其容易发生接口不匹配，导致集成失败。

核心原则：**多任务并行时，必须将共用的接口契约或类型定义作为高优先级的独立先行任务予以锁定。**

具体操作分三步：

1. **写定类型声明**：单独跑一个任务，把 `src/types/notification.ts` 里的接口声明写完并 review 通过。这个任务很小，但它是所有下游任务的地基。
   ```typescript
   // src/types/notification.ts（先行任务的交付物）
   export interface NotificationService {
     sendRefundConfirmation(params: {
       conversation_id: string;
       amount_cents: number;
       recipient_email: string;
     }): Promise<{ notification_id: string }>;
   }
   ```
2. **多方消费导入**：下游任务都直接 import 这个类型文件。TypeScript 编译器在进行静态类型检查时就能保证调用方和实现方的契约一致。
3. **任务单显式声明**：在 `./TODOS.md` 里将依赖关系进行显式声明，让下游任务在开工前自动识别前置锁定状态。


## 中途发现边界划错了怎么办

Agent 开始实现后，有时会发现原来的任务边界有问题。例如 Agent 在实现 `RefundService.processRefund` 时，发现调用支付网关的 `PaymentGateway` 接口接受浮点数金额，违反了 `./AGENTS.md` 的“整数分”红线。要完成 Task A，必须先修改网关接口，但该接口不在 Task A 的修改范围里。

允许 Agent 在当前会话中“顺手修改”职责范围之外的模块，是破坏交付物整洁度的典型高危行为。它不仅会产生逻辑蔓延，还会污染 Pull Request 的 diff 边界，极大抬高集成阶段的评审成本。

正确的处理步骤如下：

1. **限制范围，挂起待办**：暂停当前 Agent，在 `./TODOS.md` 中记录一个高优先级的独立重构任务 `RELAY_PAYMENT_GATEWAY_REFACTOR`。
2. **前置合并，锁定接口**：由单独的会话先完成并合入该网关重构任务，锁定新版接口。
3. **重启会话，拉回状态**：重启 Task A 的 Agent 会话，将新版接口作为已知的前提条件注入，继续实现原任务。


## 工件可追溯性：从代码追踪设计意图

在工程生命周期中，我们常在代码中遇到看似冗余的校验分支。如果缺乏工件可追溯链，后续接手的 Agent 可能会因为看不懂其技术意图，而将其当作历史包袱误删，导致生产环境故障。

工件追溯（Artifact Traceability）要求建立一条“需求规约（Spec 编号）→ 设计决策（ADR 段落）→ 任务卡（Task ID）→ 物理代码（Commit Hash）→ 测试套件”的闭环追溯链条。

这一链条的维护无需复杂的管理工具，只需在开发约定中强制维护一个轻量级的关联契约。例如，Spec 中的条款被打上唯一的编号 `REFUND_GATE_01`，后续所有的衍生设计、任务定义以及 Git Commit Message 都必须携带该编号作为前缀。即使在数月后，任何冷启动的 Agent 都能通过提交哈希反向追溯到最初的架构设计意图。


## gstack 参考实现

在 `gstack` 体系中，锁定方案的自动化拆解与追踪是由 `/autoplan` 工作流与内置的 `/codex` 共享元数据库共同承载的。

当设计文档合并至主分支后，工程师通过调用 autoplan 技能，自动提取系统时序图与接口定义，并将其编译为规范的原子任务卡：

```bash
# 自动拆解方案并派生出带依赖关系的任务队列
$ gstack autoplan --design ./docs/designs/auto_refund_v1.md
```

拆解出的原子任务会被自动推送到大仓根目录下的 `./TODOS.md` 中，每一项任务都通过唯一的 Task ID 与 `./.gstack/codex.json` 数据库相绑定。这套编排与状态追踪方案构成了可替换的物理实现层，其根本价值在于确保并行运转的多线 Agent 具备确定的接口契约与排他性的修改边界。


## 典型反模式

* **将高耦合的超级大任务派发给 Agent**：这会导致会话迅速耗尽上下文窗口并引发逻辑漂移，产生无法通过验收的半成品。
* **边界模糊的多任务并发**：多线任务同时获得对同一个共享模块的随意修改权限，导致并行 Agent 互相覆写代码。
* **孤立的代码提交**：提交记录只写着 "fix: refund"，无 Task ID 也无 Spec 引用，导致代码的业务意图在合并后彻底丢失。
* **中途在当前会话扩大范围**：Agent 发现需要改动范围外的模块时，在当前会话里“顺手改掉”。这让单次 PR 的 diff 横跨两个不同职责的改动，出了问题难以定位。


## 本章要点

* 边界清楚与单一职责是任务拆解的前提，任务的物理粒度决定了并行的效率。
* 判断粒度是否合适，看三个指标：能独立验证、不跨越多个架构层、Agent 不需要反复猜测意图。
* 利用相对路径下的配置文档约束任务的输入输出与修改文件范围，防止 Agent 跨模块乱改。
* 跨任务依赖的解法是把接口类型定义作为独立先行任务锁定，让所有下游任务都 import 同一份契约文件。
* 中途发现边界划错时，暂停当前会话，将新问题记入 `./TODOS.md`，先解决前置依赖再重启。
* 通过 Spec 编号一路透传至 Commit Message，建立轻量级、无工具负担的工件可追溯链。

---

> 任务边界划清、接口契约先行，并行实现的前置就绪。下一部分进入 Implement 实现：把这些任务放进一个没有上下文墙、有强制边界、可安全并行、可观测的高产环境里落地。
